# The following section is auto-generated by flutter gen-l10n and should not be modified manually.
flutter:
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icon/
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here, it is
  # recommended to specify first the family name, and then include a list of flowing Fonts
  # For example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  # For details regarding fonts from package dependencies, see
  # https://flutter.dev/custom-fonts/#from-packages

name: flutter_streetphare
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none"

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as product and file version while build-number is used as numeric part of file version.
version: 2.2.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically get dependencies and download them to your project, run 'flutter pub get'.
# To automatically upgrade your package dependencies to the latest versions
# compatible with your product, run 'flutter pub upgrade --major-versions'.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ^1.1.1
  web_socket_channel: ^3.0.3

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  flutter_map: ^8.3.0
  latlong2: ^0.9.0
  geolocator: ^14.0.3
  # geofence_manager: ^0.0.4
  flutter_map_tile_caching: ^10.1.1
  permission_handler: ^12.0.3
  open_settings: ^2.0.0
  shared_preferences: ^2.2.3
  flutter_background_service: ^5.0.1
  flutter_foreground_task: ^9.2.2
  http: ^1.2.1
  connectivity_plus: ^7.1.1
  url_launcher: ^6.3.2
  cryptography: ^2.9.0
  cryptography_flutter: ^2.3.4
  flutter_reactive_ble: ^5.5.0
  package_info_plus: ^10.0.0
  share_plus: ^13.1.0
  flutter_local_notifications: ^22.0.1
  mobile_scanner: ^7.2.0
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  path_provider: ^2.1.5
  sms_autofill: ^2.5.0
  logging: ^1.3.0
  intl: ^0.20.2 # Ajouté pour l'internationalisation
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_ce_generator: ^1.11.2
  build_runner: ^2.15.0
  build_test: 3.5.15
  build_web_compilers: ^4.0.0
  test: ^1.31.0
  mocktail: ^1.0.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
